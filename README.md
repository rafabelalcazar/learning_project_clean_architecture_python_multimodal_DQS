# Unimodal Data Quality Analyzer

A Python tool designed with Clean Architecture to scan datasets, evaluate unimodal files (text, images, audio), compute data quality metrics, and export them to CSV.
